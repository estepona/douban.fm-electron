import * as general from './general';
import * as locale from './locale';
import * as shortcut from './shortcut';
import { ipcChannels } from './ipc_channel';

export { general, ipcChannels, locale, shortcut };
